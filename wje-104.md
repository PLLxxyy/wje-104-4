请根据以下要求生成项目代码和 README。

## 一、硬性约束

### 1.1 构建可运行性
- 项目必须可通过 `npm install && npm run dev` 一键启动开发服务器
- Vite 开发服务器监听端口 `28604`，对应 Docker 宿主机映射 `28604:5173`
- 启动后浏览器访问 `http://localhost:28604` 能直接看到首页，无需任何额外配置
- 构建命令 `npm run build` 必须无报错生成 `dist/` 目录
- TypeScript 编译零错误，`tsc --noEmit` 通过

### 1.2 功能完整性
- 所有核心功能必须实现，不得留空函数体或 TODO 注释占位
- Canvas 绘图必须真正可用：鼠标按下→移动→抬起能画出连续线条
- 图层系统必须实际生效：切换图层可见性后画布实时刷新
- 撤销/重做必须实际生效：操作后 Ctrl+Z 能回退，Ctrl+Y 能前进
- 作品保存和加载必须端到端打通：保存后刷新页面作品仍在
- 导出 PNG 必须生成真实可下载的图片文件

### 1.3 代码质量
- 每个文件单一职责，严禁将多个不相关逻辑塞进同一个文件
- 组件文件不超过 300 行，超出必须拆分
- 所有 TypeScript 类型必须显式声明，禁止 `any`（除极少数 DOM API 边界）
- 所有常量抽取到 `constants/` 目录，禁止硬编码魔法值
- 所有 localStorage 操作统一封装，禁止散落在各组件中直接调用 `window.localStorage`

---

## 二、项目主要需求

### 2.1 项目概述
咖啡拉花模拟器是一款基于 HTML Canvas 的创意绘图应用，模拟在咖啡杯俯视图上进行拉花创作。用户可以选择画笔工具、调节参数、在多图层画布上绘制精美拉花图案，也可以加载预设模板进行二次创作，最终将作品保存到本地或导出为 PNG 图片。

**项目类型**：Web 前端（纯前端应用，数据存 localStorage）

### 2.2 技术栈
- **前端框架**：React 18 + TypeScript
- **状态管理**：Zustand
- **构建工具**：Vite
- **核心渲染**：HTML5 Canvas 2D API
- **路由**：React Router v6
- **其他依赖**：uuid（生成唯一 ID）、dayjs（日期格式化）
- **CSS 方案**：CSS Modules 或全局 CSS 变量（不使用 UI 组件库，手写样式）

### 2.3 数据模型

#### Artwork（作品）
```typescript
interface Artwork {
  id: string;              // uuid
  name: string;            // 作品名称，用户可编辑
  layers: Layer[];         // 图层数组，从底到顶排列
  thumbnail: string;       // base64 缩略图（宽 200px 的 JPEG）
  createdAt: string;       // ISO 8601 时间戳
  updatedAt: string;       // ISO 8601 时间戳
}
```

#### Layer（图层）
```typescript
interface Layer {
  id: string;              // uuid
  name: string;            // 图层名称（如"奶泡层"、"雕花层"、"点缀层"）
  visible: boolean;        // 是否可见
  opacity: number;         // 图层不透明度 0-1
  strokes: Stroke[];       // 笔画数组
}
```

#### Stroke（笔画）
```typescript
interface Stroke {
  id: string;              // uuid
  points: Point[];         // 坐标点序列
  color: string;           // 颜色值（十六进制）
  size: number;            // 画笔大小 1-50
  opacity: number;         // 笔画不透明度 0-1
  toolType: ToolType;      // 工具类型枚举
}

interface Point {
  x: number;
  y: number;
  pressure?: number;       // 可选压感值（触控设备）
  timestamp: number;       // 时间戳，用于回放
}
```

#### PresetTemplate（预设模板）
```typescript
interface PresetTemplate {
  id: string;
  name: string;            // 模板名称
  description: string;     // 简短描述
  thumbnail: string;       // base64 缩略图
  layerData: Layer[];      // 模板图层数据（预定义的笔画集合）
}
```

### 2.4 核心页面

#### 首页（Home）
- 展示应用标题、简介和咖啡杯动画
- "开始创作"按钮跳转编辑器页
- "我的作品"按钮跳转作品集页
- 展示最近 3 个作品缩略图快速入口

#### 编辑器页（Editor）
- 左侧：工具面板（ToolPanel）— 画笔/橡皮擦/填充工具切换、画笔大小滑块、颜色选择、透明度调节、流量速度调节
- 中央：画布工作区（CanvasWorkspace）— 显示咖啡杯俯视图底图，可绘制拉花图案
- 右侧：图层面板（LayerPanel）— 图层列表、新建/删除图层、调整顺序、切换可见性、调节图层透明度
- 顶部工具栏：撤销/重做按钮、预设模板入口、保存按钮、导出按钮、返回首页按钮
- 底部状态栏：当前画笔信息、画布坐标、缩放比例

#### 作品集页（Gallery）
- 网格布局展示所有已保存作品（ArtworkCard 组件）
- 每个卡片显示：缩略图、作品名称、创建日期、操作按钮（编辑/删除）
- 支持按创建时间排序（最新在前）
- 删除操作需要二次确认（ConfirmDialog）

### 2.5 横切关注点

#### 横切关注点 1：主题切换
- **触达层**：Zustand `useThemeStore` → CSS 变量定义 → `theme.css` 全局主题文件 → 各组件样式引用 CSS 变量
- 支持浅色（拿铁白）和深色（浓缩棕）两种主题
- 主题偏好持久化到 localStorage
- 切换时无页面闪烁，通过 `data-theme` 属性驱动 CSS 变量切换
- 画布背景色跟随主题变化

#### 横切关注点 2：全局错误处理
- **触达层**：`ErrorBoundary` 全局组件包裹 App 根节点 → Canvas 操作内部 try-catch → 导出失败 toast 提示 → localStorage 读写异常降级处理
- Canvas 绑定和渲染过程中捕获异常，显示友好错误信息而非白屏
- 导出 PNG 失败时弹出提示而非静默失败
- localStorage 满时提示用户清理空间

### 2.6 共享枚举/常量

#### 常量组 1：ToolType（工具类型枚举）
- **定义位置**：`constants/enums.ts`
- **使用位置**：`Stroke` 数据模型、`ToolPanel` 组件、`useCanvas` hook
- **枚举值**：
  - `brush` — 画笔
  - `eraser` — 橡皮擦
  - `fill` — 填充工具

#### 常量组 2：PresetColor（预设颜色）
- **定义位置**：`constants/colors.ts`
- **使用位置**：颜色面板组件、Canvas 绘制逻辑、Stroke 数据
- **颜色值**：
  - `espresso`（浓缩咖啡色）：`#3E2723`
  - `cream`（奶白色）：`#FFF8E1`
  - `foam`（奶泡色）：`#EFEBE9`
  - `milk`（牛奶色）：`#F5F5F5`

### 2.7 共享前端组件

#### `<CanvasWorkspace>`
- **职责**：主画布区域，负责渲染咖啡杯底图和所有可见图层的笔画
- **使用页面**：编辑器页（Editor）、预览模式
- **Props**：`width`, `height`, `layers`, `activeLayerId`, `tool`, `onStrokeComplete`

#### `<ToolPanel>`
- **职责**：工具参数调节面板，包含画笔大小、颜色、透明度、流量速度等控件
- **使用页面**：编辑器页（Editor）
- **Props**：`tool`, `brushSize`, `color`, `opacity`, `flowSpeed`, `onChange`

#### `<LayerPanel>`
- **职责**：图层管理面板，支持图层列表展示、新建、删除、排序、显隐切换
- **使用页面**：编辑器页（Editor）
- **Props**：`layers`, `activeLayerId`, `onSelect`, `onToggleVisibility`, `onAdd`, `onDelete`, `onReorder`

#### `<ArtworkCard>`
- **职责**：作品卡片组件，展示单个作品的缩略图、名称、日期和操作按钮
- **使用页面**：作品集页（Gallery）、首页（Home）
- **Props**：`artwork`, `onEdit`, `onDelete`

### 2.8 共享 Hooks

#### `useCanvas`
- **职责**：Canvas 绘图核心逻辑，绑定 canvas DOM 元素，处理鼠标/触控事件，执行实际绘制渲染
- **使用位置**：`CanvasWorkspace` 组件
- **返回值**：`canvasRef`（绑定到 canvas 元素）、`clearCanvas`、`exportCanvas`

#### `useHistory`
- **职责**：操作历史状态管理，维护撤销/重做栈
- **使用位置**：编辑器页、`useCanvas`
- **参数**：`maxSteps`（默认 50）
- **返回值**：`pushState`, `undo`, `redo`, `canUndo`, `canRedo`, `clear`

#### `useLocalStorage`
- **职责**：localStorage 读写封装，支持泛型、JSON 序列化/反序列化、异常降级
- **使用位置**：所有需要持久化的 store
- **返回值**：`getItem<T>`, `setItem<T>`, `removeItem`

---

## 三、文件结构强制清单

```
wjelatteart/
├── .gitignore
├── index.html
├── package.json
├── tsconfig.json
├── tsconfig.node.json
├── vite.config.ts
├── README.md
└── frontend/src/
    ├── main.tsx                          # 应用入口
    ├── App.tsx                           # 根组件（路由 + ErrorBoundary）
    ├── vite-env.d.ts                     # Vite 类型声明
    ├── api/                              # 不需要（纯前端）
    ├── stores/
    │   ├── useArtworkStore.ts            # 作品数据 store
    │   ├── useToolStore.ts               # 工具参数 store
    │   ├── useLayerStore.ts              # 图层状态 store
    │   └── useThemeStore.ts              # 主题切换 store
    ├── types/
    │   ├── artwork.ts                    # Artwork 类型定义
    │   ├── layer.ts                      # Layer 类型定义
    │   ├── stroke.ts                     # Stroke、Point 类型定义
    │   ├── preset.ts                     # PresetTemplate 类型定义
    │   └── enums.ts                      # ToolType 等枚举
    ├── components/
    │   ├── common/
    │   │   ├── ArtworkCard/              # 作品卡片组件
    │   │   │   ├── index.tsx
    │   │   │   └── styles.module.css
    │   │   ├── ThemeToggle/              # 主题切换按钮
    │   │   │   ├── index.tsx
    │   │   │   └── styles.module.css
    │   │   └── ConfirmDialog/            # 确认对话框
    │   │       ├── index.tsx
    │   │       └── styles.module.css
    │   └── editor/
    │       ├── CanvasWorkspace/          # 主画布组件
    │       │   ├── index.tsx
    │       │   └── styles.module.css
    │       ├── ToolPanel/                # 工具面板组件
    │       │   ├── index.tsx
    │       │   └── styles.module.css
    │       ├── LayerPanel/               # 图层面板组件
    │       │   ├── index.tsx
    │       │   └── styles.module.css
    │       ├── PresetGallery/            # 预设模板库组件
    │       │   ├── index.tsx
    │       │   └── styles.module.css
    │       └── ExportDialog/             # 导出对话框组件
    │           ├── index.tsx
    │           └── styles.module.css
    ├── hooks/
    │   ├── useCanvas.ts                  # Canvas 绘图逻辑
    │   ├── useHistory.ts                 # 撤销/重做逻辑
    │   └── useLocalStorage.ts            # localStorage 封装
    ├── pages/
    │   ├── Home/
    │   │   ├── index.tsx
    │   │   └── styles.module.css
    │   ├── Editor/
    │   │   ├── index.tsx
    │   │   └── styles.module.css
    │   └── Gallery/
    │       ├── index.tsx
    │       └── styles.module.css
    ├── router/
    │   └── index.tsx                     # 路由配置
    ├── constants/
    │   ├── enums.ts                      # ToolType、LayerType 等枚举
    │   ├── colors.ts                     # PresetColor 颜色常量
    │   └── presets.ts                    # 5 个预设拉花模板数据
    ├── utils/
    │   ├── canvas.ts                     # Canvas 工具函数（坐标转换、碰撞检测）
    │   ├── export.ts                     # 导出 PNG 工具函数
    │   └── thumbnail.ts                  # 缩略图生成工具函数
    └── styles/
        ├── theme.css                     # 主题 CSS 变量定义
        └── global.css                    # 全局基础样式
```

**严禁合并职责到单一文件**。例如：
- `useArtworkStore.ts` 只管作品 CRUD，不要把工具状态也放进去
- `CanvasWorkspace` 只负责画布渲染，不要把工具选择逻辑也塞进去
- `constants/enums.ts` 只放枚举定义，不要把颜色值混进来（颜色在 `colors.ts`）
- 每个组件必须有独立的 `styles.module.css`，禁止将所有样式写在 `global.css`

---

## 四、核心流程

### 4.1 编辑器核心流程
```
打开首页 → 点击"开始创作" → 进入编辑器页
  ↓
编辑器初始化：
  1. 创建默认画布（800×800）
  2. 加载咖啡杯底图（CSS 径向渐变模拟）
  3. 创建默认图层"奶泡层"
  4. 初始化工具状态（画笔、默认奶白色、大小 10）
  ↓
选择工具和参数：
  1. 在 ToolPanel 选择画笔/橡皮擦/填充
  2. 调节画笔大小（1-50 滑块）
  3. 选择颜色（预设色或自定义）
  4. 调节透明度（0-100%）
  5. 调节流量速度（影响笔触密度）
  ↓
绘制拉花图案：
  1. 鼠标按下 → 记录起点，开始新笔画
  2. 鼠标移动 → 采集坐标点，实时渲染到当前图层
  3. 鼠标抬起 → 结束笔画，将 Stroke 推入当前图层
  4. 每次完成笔画 → pushState 到历史栈
  ↓
图层操作：
  1. 可新建"雕花层"、"点缀层"
  2. 切换当前活跃图层
  3. 调整图层可见性和透明度
  4. 删除图层（需确认，至少保留一个图层）
  ↓
保存作品：
  1. 弹出命名对话框，输入作品名称
  2. 生成缩略图（canvas 导出 200px 宽 JPEG）
  3. 创建 Artwork 对象写入 localStorage
  4. 显示"保存成功"提示
  ↓
导出 PNG：
  1. 点击导出按钮，弹出 ExportDialog
  2. 选择导出尺寸（1x/2x/4x）
  3. 合并所有可见图层到临时 canvas
  4. 调用 canvas.toBlob 生成 PNG
  5. 创建 <a download> 触发下载
```

### 4.2 预设模板加载流程
```
点击"预设模板"按钮 → 打开 PresetGallery 弹窗
  ↓
展示 5 个模板卡片（缩略图 + 名称 + 描述）
  ↓
点击某个模板 → 确认是否清空当前画布
  ↓
将模板的 layerData 深拷贝到当前作品的 layers
  ↓
画布重新渲染所有图层
```

### 4.3 作品管理流程
```
进入作品集页 → 从 localStorage 读取所有 Artwork
  ↓
按 updatedAt 降序排列，网格展示 ArtworkCard
  ↓
点击"编辑" → 携带 artworkId 跳转编辑器页 → 从 localStorage 加载对应作品
  ↓
点击"删除" → ConfirmDialog 二次确认 → 从 localStorage 移除 → 列表刷新
```

---

## 五、测试样例数据

### 5.1 预设拉花模板（5 个）

在 `constants/presets.ts` 中硬编码以下 5 个预设模板数据，每个模板包含预定义的 `layerData`（Stroke 数组）：

1. **经典叶子（Leaf）**
   - 描述：最经典的基础拉花图案，从杯中心向两侧展开叶片形状
   - 颜色：cream（奶白色）在 espresso（浓缩色）背景上
   - 包含约 15-20 个贝塞尔曲线笔画，模拟叶片脉络

2. **心形（Heart）**
   - 描述：浪漫的心形拉花，适合表达爱意
   - 颜色：cream + foam 双色
   - 包含约 10-12 个笔画，勾勒心形轮廓和内部纹理

3. **天鹅（Swan）**
   - 描述：优雅的天鹅造型，拉花进阶图案
   - 颜色：cream 为主，milk 点缀
   - 包含约 20-25 个笔画，包含颈部曲线和翅膀展开

4. **玫瑰（Rose）**
   - 描述：层叠花瓣组成的玫瑰花拉花
   - 颜色：cream + foam 渐变
   - 包含约 25-30 个笔画，多层花瓣叠加

5. **羽毛（Feather）**
   - 描述：轻盈飘逸的羽毛图案
   - 颜色：cream 在 espresso 上
   - 包含约 12-15 个笔画，主轴线加两侧细密分支

**实现要求**：每个模板的 `layerData` 必须包含真实的 `Stroke` 数据（坐标点数组），而非空白占位。坐标基于 800×800 画布，用数学函数（正弦、余弦、贝塞尔）生成平滑曲线点。

### 5.2 示例作品（2 个）

在首次加载时（localStorage 中无数据时），自动注入 2 个示例作品：

1. **示例作品 1："晨光叶片"**
   - 内容：基于叶子模板的轻微修改版
   - 包含 2 个图层（奶泡层 + 雕花层）
   - `createdAt` 设为 3 天前

2. **示例作品 2："天鹅湖畔"**
   - 内容：基于天鹅模板的轻微修改版
   - 包含 3 个图层（奶泡层 + 雕花层 + 点缀层）
   - `createdAt` 设为 1 天前

---

## 六、代码存放规则

- 所有源代码放在 `frontend/src/` 下
- 项目根目录只有配置文件（`package.json`、`vite.config.ts`、`tsconfig.json` 等）
- `frontend/src/stores/` 存放 Zustand store，每个 store 一个文件
- `frontend/src/types/` 存放 TypeScript 接口和类型定义，每个数据模型一个文件
- `frontend/src/components/` 按功能域分子目录（`common/`、`editor/`），每个组件独立文件夹含 `index.tsx` 和 `styles.module.css`
- `frontend/src/hooks/` 存放自定义 hooks，每个 hook 一个文件
- `frontend/src/pages/` 存放页面级组件，每个页面独立文件夹
- `frontend/src/constants/` 存放枚举、颜色常量、预设模板数据
- `frontend/src/utils/` 存放纯工具函数
- `frontend/src/styles/` 存放全局样式和主题变量
- 禁止在组件文件中直接定义常量，必须引用 `constants/` 下的定义
- 禁止在 store 文件中直接定义类型，必须引用 `types/` 下的定义

---

## 七、部署要求

本项目为纯前端项目，无需 Docker Compose 和数据库。提供以下运行方式：

### 7.1 开发模式（主要方式）
```bash
cd frontend
npm install
npm run dev
# 访问 http://localhost:28604
```

### 7.2 Vite 端口配置
在 `frontend/vite.config.ts` 中配置：
```typescript
export default defineConfig({
  server: {
    port: 28604,
    host: '0.0.0.0',
  },
  // ...
})
```

### 7.3 生产构建
```bash
cd frontend
npm run build
# 使用任意静态文件服务器托管 dist/ 目录
```

### 7.4 可选 Nginx Docker 部署
- 提供 `frontend/Dockerfile`，基于 `nginx:alpine` 镜像
- `nginx.conf` 配置 `try_files $uri $uri/ /index.html;` 支持 SPA 路由
- 暴露端口 `28604`

---

## 八、README 写法

根目录必须有 `README.md`，用中文说明项目。

README 必须包含以下章节：

1. **项目名称和一句话介绍**
   - "咖啡拉花模拟器 —— 在数字画布上创作你的专属拉花艺术"

2. **项目主要功能**
   - 列出 6-8 个核心功能点

3. **快速启动方式**
   ```bash
   cd frontend
   npm install
   npm run dev
   # 访问 http://localhost:28604
   ```

4. **访问地址**
   - 开发模式：`http://localhost:28604`

5. **技术栈**（表格展示）

   | 技术 | 版本 | 用途 |
   |------|------|------|
   | React | 18 | UI 框架 |
   | TypeScript | 5.x | 类型安全 |
   | Zustand | 4.x | 状态管理 |
   | Vite | 5.x | 构建工具 |
   | React Router | 6.x | 路由管理 |
   | HTML Canvas | - | 绘图渲染 |

6. **项目目录结构**（代码块展示）

7. **使用说明**
   - 画布操作（鼠标绘制、触控支持）
   - 工具切换（画笔/橡皮擦/填充）
   - 参数调节（画笔大小、颜色、透明度）
   - 图层管理（新建/删除/显隐/排序）
   - 预设模板加载
   - 撤销/重做操作
   - 保存和导出

8. **License**

README 要求：
- 命令可直接复制运行
- 目录结构用代码块展示
- 技术栈用表格展示
- 不要保留脚手架默认 README 内容

---

## 九、Git 要求

代码编写完成后初始化 Git + 创建 `.gitignore`：

```bash
git init
git add -A
git commit -m "init: 咖啡拉花模拟器项目初始化"
```

`.gitignore` 内容：
```
node_modules/
dist/
.env
.DS_Store
*.log
.idea/
.vscode/
```
